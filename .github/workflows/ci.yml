name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    - name: Security scan with Bandit
      run: |
        # Exclude tests and coverage artifacts to avoid false positives (e.g., B101 asserts)
        bandit -r . -x tests,tests_strict,htmlcov -f json -o bandit-report.json
    - name: Security scan with Safety
      run: |
        safety check --output json > safety-report.json
    - name: Run tests
      run: |
        pytest --cov=app --cov=bmi_core --cov=bodyfat --cov-report=xml
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
